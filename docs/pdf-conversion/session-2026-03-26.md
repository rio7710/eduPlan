# Session 2026-03-26

이 문서는 2026-03-26 PDF 변환 작업 handoff입니다.

## Current Stage

- `Stage 1`: 무누락 원문 데이터 수집과 1차 검증
- `Stage 2`: 레이아웃 구조 해석 시작 전 상태

## Stage 1 Done

- PDF 기본 변환 결과를 `edufix_trans/<파일명>_temp` 아래에 저장
- 출력 폴더 구성
  - `md`
  - `img`
  - `layout`
  - `report`
- 파일명 stage prefix 적용
  - `s01_*.md`
  - `s02_*_layout.json`
  - `s03_*_report.json|md`
- 레이아웃 추출에 box/shape 포함
- box 안 텍스트 관계(`containerIds`, `containedBlockIds`) 저장
- 1차 누락 검증 추가
  - `missingPageCount`
  - `missingTextBlocks`
  - `missingBoxedTextBlocks`
- 10강 PDF 기준 제목/부제 reading order 보정 완료
- HTML `<table>` 렌더는 preview에서 동작하도록 반영 완료

## Stage 1 Notes

- 10강 PDF에서 `제10강 ...` 텍스트는 누락이 아니라 순서 문제였고 수정 완료
- box 테두리도 1페이지에서 실제 검출 확인
- 이미지 내부 텍스트는 현재 `누락`이 아니라 `이미지로 보존` 상태

## Review Screen Status

- legacy DB review rows는 무시하도록 변경
- 현재 review 화면은 새로 생성한 후보만 표시
- 문장 review 카드 동작
  - 원문: 읽기 전용
  - 수정안: 편집 가능
  - 승인 / 수정승인 / 거부 가능
- 카드 내부 overflow 스크롤 추가

## Important Limitation

- 기본 변환 직후 review 후보는 아직 `줄끊김 복원` 임시 규칙 기반
- 현재 후보 생성기는 과검출이 있어 Stage 2 전 정교화 필요
- 제목/소제목까지 후보로 잡히는 문제를 더 줄여야 함

## LightGBM Work

- 새 폴더: `scripts/LightGBM_ML`
- 포함 파일
  - `line_break_synth.py`
  - `line_break_features.py`
  - `train_line_break_model.py`
  - `README.md`
- artifact 생성 완료
  - `artifacts/line_break_train.jsonl`
  - `artifacts/line_break_metrics.json`
  - `artifacts/line_break_model.txt`
- 현재 synthetic baseline 학습 결과
  - `validAccuracy: 0.9852`

## Next Step

- Stage 2 시작
- 목표: 레이아웃 구조 해석과 줄끊김 후보 정교화
- 우선순위
  - 제목/소제목 제외 규칙 강화
  - 줄끊김 후보를 `merge_space / merge_no_space / keep_break`로 분리
  - LightGBM 모델을 후보 생성기에 연결
  - review 화면에는 줄바꿈 위주 후보만 노출

## Run Commands

```powershell
cd d:\OneDrive\Desktop\eduPlan\eduFixer
npm run dev
```

```powershell
.\.venv\Scripts\python.exe .\scripts\LightGBM_ML\train_line_break_model.py
```
