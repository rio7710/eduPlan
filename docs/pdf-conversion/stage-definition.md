# Stage Definition

Current numbering starts at `Stage 1`.

- `Stage 1`: omission-safe source capture and first validation
- `Stage 2`: layout interpretation and separate-block analysis
- `Stage 3`: noise cleanup
- `Stage 4`: sentence segmentation
- `Stage 5`: structure labeling
- `Stage 6`: AI refinement
- `Stage 7`: user revision and training capture
- `Stage 8`: final export and feedback loop

Deprecated:
- older `Stage 0` references

Current implementation status:
- completed work so far belongs to `Stage 1`
- current next focus belongs to `Stage 2`
